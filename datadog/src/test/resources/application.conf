reactivemongo {
  datadog {
    name = "tests"

    hostname = "localhost"
    port = 8126

    # Optionals
    prefix = "mytests"
    constantTags = [ "bar", "lorem" ]

    bufferPoolSize = 10
    entityID = "id"
    queueSize = 12
    senderWorkers = 13
    socketBufferSize = 14
    timeout = 2000

    telemetry {
      enabled = true
      hostname = "localhost"
      port = 1234
    }
  }
}